{
  "name": "project-4-test-plan-agent",
  "version": "1.0.0",
  "type": "module",
  "description": "Intelligent Test Planning Agent with Vite Frontend and Node.js Serverless Backend",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "devDependencies": {
    "@vercel/node": "^5.6.22"
  }
}
